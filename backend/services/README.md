# Services

Service modules will host application and domain orchestration logic as the AI Drama System expands.

The initial milestone keeps business rules minimal, so no dedicated service modules are required yet.
